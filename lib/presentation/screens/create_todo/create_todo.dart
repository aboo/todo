import 'package:flutter/material.dart';

class CreateToDoScreen extends StatelessWidget {
  const CreateToDoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('create todo form'),
    );
  }
}
